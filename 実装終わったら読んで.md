apps/frontend/src/routes/AppRoutes.tsx
↑Next.jsのPageRouter使いたい。RemixのV2でも

サブスクリプションも使えるようにしておきたい
インフラ構成上WS使えないのでgraphql-sseでインターフェイスをそろえたクラスを作ってる

認証周りが既存と離れすぎてるかも
lib/cognito/作って、そこにセッション情報取得とサインイン関数を作って、内部をローカルストレージを読むだけの単純実装にして。
バックエンドはIDトークンのCognitoIDを使って、CognitoIdで引いてました。